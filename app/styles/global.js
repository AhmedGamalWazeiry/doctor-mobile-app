import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  textInput: {
    borderColor: "#aaa",
    borderWidth: 1,
    borderRadius: 5,
    width: "100%",
    padding: 10,
    marginBottom:10,
  },
});
